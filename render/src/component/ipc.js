import React from 'react';

const {ipcRenderer} = window.require('electron') || {};
//

class IPC extends React.PureComponent {
    state ={}
    render() {
        return null
    }
}

export default IPC
